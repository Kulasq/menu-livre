const CONFIG = {
  // URL base da API — troque para o domínio em produção
  API_URL: 'http://localhost:8000',

  // Chave usada no localStorage
  STORAGE_KEYS: {
    CARRINHO:  'pdm_carrinho',
    CLIENTE:   'pdm_cliente',
    TOKEN:     'pdm_token',
  },
};